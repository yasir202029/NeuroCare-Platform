# Mobile application

`apps/mobile` is an Expo TypeScript app for iOS, Android, and web. Build and submit native binaries with Expo/EAS from the mobile release environment; Vercel hosts web deployments only. The app never uses a service-role key or stores clinical data locally. Before enabling recording or AI actions, the API must verify the current server-side consent record and audit the action.
