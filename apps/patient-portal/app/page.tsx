"use client";
import { FormEvent, useState } from "react";
export default function Page() { const [status,setStatus]=useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data=new FormData(event.currentTarget); const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? ""}/api/auth/request-link`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:data.get("email"),portal:"patient"})}); setStatus(response.ok ? "Check your inbox for your secure sign-in link." : "Secure sign-in is not configured yet. Your details were not sent."); } return <main><header><p className="eyebrow">NeuroAssess · Patient</p><h1>Patient sign in</h1><p>Enter your email to receive a secure sign-in link to your personal care portal.</p></header><section className="hero"><h2>Your portal is assigned from your verified role.</h2><p>Patients reach their own care portal. Clinicians reach their authorised clinical workspace, including availability and assigned appointments. The browser never chooses a role.</p></section><section><form onSubmit={submit}><label>Work or personal email<input name="email" required type="email" autoComplete="email" /></label><button type="submit">Send secure sign-in link</button></form>{status&&<p className="notice">{status}</p>}</section></main>; }
