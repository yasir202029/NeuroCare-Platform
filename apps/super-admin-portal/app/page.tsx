"use client";
import {useState} from "react";
const sections=["Dashboard","Tenants","Users","Governance","Audit","Settings"] as const;const controls=[["Tenant health","0","Active tenants"],["Access governance","0","Access exceptions"],["AI policy","0","Policy reviews"],["Feature controls","0","Changes awaiting approval"],["Audit intelligence","0","Critical events"]];
export default function Page(){const[a,setA]=useState<(typeof sections)[number]>("Dashboard");const[d,setD]=useState(false);return <main className={`portal ${d?"dark":""}`}><aside className="sidebar"><div className="brand"><span>n</span><div><b>NeuroAssess</b><small>Platform control</small></div></div><nav>{sections.map(x=><button key={x} className={a===x?"active":""} onClick={()=>setA(x)}>{x}</button>)}</nav><div className="side-foot"><button onClick={()=>setD(!d)}>{d?"Light appearance":"Dark appearance"}</button></div></aside><section className="content"><header><div><p className="eyebrow">SaaS platform administration</p><h1>Control without compromise.</h1><p className="muted">Tenant operations, policy and audit controls are isolated from clinical workflows.</p></div><div className="profile">SA</div></header>{a==="Dashboard"?<><section className="hero"><div><p className="eyebrow">Platform status</p><h2>Every tenant, clearly governed.</h2><p>Manage clinic boundaries, global policy, secure role design and audited platform configuration through one control plane.</p></div><div className="hero-status"><span>0</span><small>alerts requiring review</small></div></section><section className="cards">{controls.map(([title,value,status])=><article className="card" key={title}><p className="eyebrow">{status}</p><span className="badge">{value}</span><h3>{title}</h3><p className="muted">Platform settings remain gated by authorised super-admin permissions.</p></article>)}</section></>:<section className="workspace"><p className="eyebrow">{a}</p><h2>{a} control centre</h2><p className="muted">All platform actions are logged, tenant-aware and separated from patient and clinician workflows.</p><div className="empty">No information is available yet.</div></section>}<footer>Never expose secrets or clinical records in platform settings. All changes must be authorised and auditable.</footer></section></main>}
