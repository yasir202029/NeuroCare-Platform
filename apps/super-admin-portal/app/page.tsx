"use client";
import { useState } from "react";
const controls = ["Clinic boundaries", "Role permissions", "AI provider policy", "Feature flags", "Audit and security"];
export default function Page() { const [selected, setSelected] = useState(controls[0]); return <main><header><p className="eyebrow">NeuroAssess · System</p><h1>System control plane</h1><p>Super-admin controls are isolated from patient and clinician workflows.</p></header><section className="grid"><aside>{controls.map((control) => <button className={selected===control?"active":""} onClick={()=>setSelected(control)} key={control}>{control}</button>)}</aside><article><p className="eyebrow">System policy</p><h2>{selected}</h2><p>Changes to {selected.toLowerCase()} require an authorised super-admin, an audited action, and an approved production configuration. No environment secret is displayed in this portal.</p><div className="notice">Configure actual providers, domains, retention, roles and feature rules only after reviewing the organisation’s clinical governance requirements.</div></article></section></main>; }
