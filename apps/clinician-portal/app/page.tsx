"use client"; import {useState} from "react";
const tabs=["Today","My patients","Calendar","Review queue"];
export default function Page(){const[t,setT]=useState("Today");return <div className="shell"><aside className="sidebar"><div className="brand">NeuroAssess<small>Clinician workspace</small></div><nav>{tabs.map(x=><button className={t===x?"active":""} onClick={()=>setT(x)} key={x}>{x}</button>)}</nav><footer>Only assigned patient records appear here.<br/>All activity is auditable.</footer></aside><main className="content"><header className="header"><div><p className="eyebrow">Clinical workspace</p><h1>Focus on care.</h1></div><button className="dark-toggle" onClick={()=>setT("Calendar")}>Set availability</button></header>{t==="Today"?<><section className="hero"><p className="eyebrow">Today</p><h2>Your clinical day, organised.</h2><p>Appointments, new information and follow-up tasks are grouped here. Open a patient file only from your assigned list.</p></section><div className="grid"><section className="surface span-6"><p className="eyebrow">Appointments</p><h2>No appointments assigned</h2><p className="muted">Confirmed clinic appointments synchronise here.</p></section><section className="surface span-6"><p className="eyebrow">New information</p><h2>No new submissions</h2><p className="muted">Patient uploads, forms and follow-up responses appear here for review.</p></section></div></>:t==="Calendar"?<section className="surface"><p className="eyebrow">Availability</p><h2>Shape your clinic calendar</h2><p className="muted">Add availability once your clinician account is connected. Scheduling staff see your approved times; patients only see confirmed appointments.</p><button className="action">Add availability</button></section>:<section className="surface"><p className="eyebrow">{t}</p><h2>{t==="My patients"?"Assigned patient files":"Clinical review"}</h2><div className="empty">No records are connected. This view stays empty until your verified clinic assignment returns data.</div></section>}</main></div>}
