"use client";
import { useState } from "react";
export default function Page() { const [panel, setPanel] = useState<"consent"|"requests"|"ai">("consent"); return <main><header><p className="eyebrow">NeuroAssess · Clinician</p><h1>Clinical command centre</h1><p>Consent, care requests and AI safeguards remain visible before any clinical workflow is opened.</p></header><nav>{(["consent","requests","ai"] as const).map((item) => <button className={panel === item ? "active" : ""} onClick={() => setPanel(item)} key={item}>{item === "ai" ? "AI safeguards" : item === "requests" ? "Care requests" : "Consent status"}</button>)}</nav>{panel === "consent" && <section><p className="eyebrow">Patient safety</p><h2>Consent status appears with every patient record.</h2><div className="guard"><strong>Recording</strong><span>Blocked until current patient consent is verified server-side</span></div><div className="guard"><strong>AI clinical assistance</strong><span>Blocked until current patient consent is verified server-side</span></div><p className="notice">No patient is selected. Consent is never inferred, cached as a clinical decision, or substituted with demo data.</p></section>}{panel === "requests" && <section><p className="eyebrow">Care coordination</p><h2>Medication and review requests</h2><p>Authenticated requests for medication refills, follow-ups, and medication reviews will show their current status and last clinical review date here.</p><div className="empty">No connected care requests</div></section>}{panel === "ai" && <section><p className="eyebrow">Guarded assistance</p><h2>AI supports clinical work. It does not decide care.</h2><ul><li>Verify patient consent before each recording or AI invocation.</li><li>Audit the provider, purpose, clinician, and source record.</li><li>Require clinician review before a note, letter, or report is saved.</li><li>Never send data to an external provider without the configured policy and consent checks.</li></ul></section>}<footer>These controls require Supabase authentication, tenant RLS, and the consent migrations before live clinical data can be connected.</footer></main>; }
