"use client";
import {useState} from "react";
export default function Page(){const [panel,setPanel]=useState("intake");return <main><header><p className="eyebrow">NeuroAssess · Clinician</p><h1>Assigned care workspace</h1><p>See appointment intake and questionnaire progress only for records assigned to you within your clinic.</p></header><nav>{["intake","questionnaires","consent","requests","files"].map(x=><button className={panel===x?"active":""} onClick={()=>setPanel(x)} key={x}>{x}</button>)}</nav><section>{panel==="intake"&&<><h2>Appointment intake</h2><p>For each assigned appointment, review the patient’s appointment type, preferred date, contact preference, reason for appointment, and intake acknowledgement before the consultation.</p><div className="empty">No assigned appointments are connected</div></>}{panel==="questionnaires"&&<><h2>Linked questionnaires</h2><p>Review ADHD or Autism questionnaires and clinician-issued informant reports linked to your assigned appointment. Responses require clinical review.</p></>}{panel==="consent"&&<><h2>Consent before recording or AI use</h2><p>Recording and AI assistance remain blocked until current patient consent is verified server-side.</p></>}{panel==="requests"&&<><h2>Refill and follow-up requests</h2><p>Review request status and last clinical review date for your assigned patients.</p></>}{panel==="files"&&<><h2>Record files</h2><p>Upload and view files only for authorised patient records.</p></>}</section></main>}
