"use client"; import {useState} from "react";
export default function Page(){const [panel,setPanel]=useState("consent");return <main><header><p className="eyebrow">NeuroAssess · Clinician</p><h1>Secure clinical workspace</h1><p>Access is limited to your authorised clinic and assigned records.</p></header><nav>{["consent","questionnaires","requests","files"].map(x=><button className={panel===x?"active":""} onClick={()=>setPanel(x)} key={x}>{x}</button>)}</nav><section>{panel==="consent"&&<><h2>Consent before recording or AI use</h2><p>Recording and AI assistance are blocked until current patient consent is verified server-side.</p></>}{panel==="questionnaires"&&<><h2>Questionnaires and informant reports</h2><p>Assign validated questionnaires and issue one-time, expiring informant links only to an authorised patient record. Informant responses are reviewed before clinical use.</p></>}{panel==="requests"&&<><h2>Refill and follow-up requests</h2><p>Review requests, status, and last clinical review date after secure care-request storage is connected.</p></>}{panel==="files"&&<><h2>Record files</h2><p>Upload clinical files to an authorised patient record only after clinic, role, and record checks complete.</p></>}</section></main>}
