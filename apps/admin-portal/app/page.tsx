"use client";
import { FormEvent, useState } from "react";
export default function Page() { const [notice,setNotice] = useState(""); const submit=(message:string)=>(event:FormEvent)=>{event.preventDefault();setNotice(message)}; return <main><header><p className="eyebrow">NeuroAssess · Admin</p><h1>Clinic operations</h1><p>Authorised administrators manage clinician invitations, clinic identity, and documents for verified patient records.</p></header><section><p className="eyebrow">Clinic brand</p><h2>Name, segment, and colour scheme</h2><form onSubmit={submit("Branding will be saved only after the selected clinic and authorised admin session are verified.")}><input required placeholder="Clinic display name"/><input required placeholder="Service segment, e.g. Adult ADHD"/><label>Primary colour<input required type="color" defaultValue="#21624f"/></label><label>Accent colour<input required type="color" defaultValue="#d99b5d"/></label><button>Save clinic brand</button></form></section><section><p className="eyebrow">Clinician access</p><h2>Add a clinician</h2><form onSubmit={submit("The clinician invitation will be issued once Supabase admin provisioning and clinic membership storage are configured.")}><input required type="email" placeholder="clinician@clinic.example"/><input required placeholder="Professional registration number"/><button>Send clinician invitation</button></form></section><section><p className="eyebrow">Documents</p><h2>Upload to an authorised record</h2><form onSubmit={submit("The file is not uploaded until a verified patient record and server-issued secure upload path are available.")}><input required type="file" accept=".pdf,.jpg,.jpeg,.png,.heic"/><button>Upload securely</button></form></section>{notice&&<p className="notice">{notice}</p>}</main>; }
