"use client";
import { useState } from "react";
export default function Page() { const [tab, setTab] = useState("operations"); return <main><header><p className="eyebrow">NeuroAssess · Admin</p><h1>Operations, with control.</h1><p>Manage the operational layer without exposing clinical records.</p></header><nav>{["operations","compliance","communications"].map((tabName) => <button className={tab===tabName?"active":""} onClick={()=>setTab(tabName)} key={tabName}>{tabName}</button>)}</nav><section><p className="eyebrow">{tab}</p><h2>{tab === "operations" ? "Clinic readiness" : tab === "compliance" ? "Compliance controls" : "Care communications"}</h2><p>{tab === "operations" ? "Staff onboarding, scheduling, billing configuration and role assignment are enabled only after the selected clinic is connected." : tab === "compliance" ? "Audit records, retention policies and consent governance appear here from the tenant-scoped security layer." : "Messages and service notifications are only visible to authorised staff after secure data services are connected."}</p><div className="empty">No connected operational data</div></section></main>; }
