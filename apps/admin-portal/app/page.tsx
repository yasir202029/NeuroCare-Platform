"use client"; import {useState} from "react";
export default function Page(){const [requested,setRequested]=useState(false);return <main><header><p className="eyebrow">NeuroAssess · Admin</p><h1>Clinic operations</h1><p>Authorised administrators can provision clinicians for their clinic with auditable, role-scoped access.</p></header><section><p className="eyebrow">Clinician access</p><h2>Add a clinician</h2><form onSubmit={(e)=>{e.preventDefault();setRequested(true)}}><label>Clinician email<input required type="email" placeholder="clinician@clinic.example"/></label><label>Professional registration<input required placeholder="Registration number"/></label><button>Send clinician invitation</button></form>{requested&&<p className="notice">The invitation will be issued only once Supabase admin provisioning and clinic membership storage are configured. No account has been created from this preview.</p>}</section><section><p className="eyebrow">Security</p><h2>Access is always auditable</h2><p>Role assignment, clinic membership, invitation acceptance, file access, physical-health updates, and consent decisions require an authorised server-side action.</p></section></main>}
