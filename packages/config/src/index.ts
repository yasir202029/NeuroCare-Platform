export function validatePublicEnvironment(environment: Record<string, string | undefined> = process.env) { const required = ["NEXT_PUBLIC_APP_URL", "NEXT_PUBLIC_SUPABASE_URL", "NEXT_PUBLIC_SUPABASE_ANON_KEY"]; const missing = required.filter((key) => !environment[key]); if (missing.length) throw new Error(`Missing required public environment variables: ${missing.join(", ")}`); return environment; }
