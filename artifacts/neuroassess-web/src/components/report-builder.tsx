import { useState } from 'react';
import { Check, ChevronRight, Columns2, FileText, History, RotateCcw, Sparkles, WandSparkles } from 'lucide-react';

type Template = { key: string; name: string; category: string; detail: string };

const templates: Template[] = [
  { key: 'adhd-initial', name: 'ADHD initial assessment', category: 'ADHD', detail: 'Full assessment findings, formulation and recommendations.' },
  { key: 'adhd-follow-up', name: 'ADHD follow-up', category: 'ADHD', detail: 'Progress, response to treatment and next steps.' },
  { key: 'adhd-titration', name: 'ADHD titration review', category: 'ADHD', detail: 'Medication response, side effects and dosage plan.' },
  { key: 'autism-assessment', name: 'Autism assessment', category: 'Autism', detail: 'Developmental history, formulation and diagnostic summary.' },
  { key: 'autism-informant', name: 'Autism informant report', category: 'Autism', detail: 'Structured account from a parent, partner or professional.' },
  { key: 'gp-letter', name: 'GP letter', category: 'Letters', detail: 'Clinically focused correspondence in GP format.' },
  { key: 'shared-care', name: 'Shared care letter', category: 'Letters', detail: 'Treatment and monitoring information for shared care.' },
  { key: 'school-letter', name: 'School support letter', category: 'Letters', detail: 'Patient-friendly support and adjustment recommendations.' },
];

export function ReportBuilder() {
  const [selected, setSelected] = useState(templates[0]);
  const [source, setSource] = useState('Transcript and clinical notes');
  const [content, setContent] = useState('## Clinical context\n\nMaya describes persistent difficulties with planning, time management and sustaining attention across work and home settings.\n\n## Assessment findings\n\nThe clinical interview and screening information should be reviewed here.\n\n## Formulation\n\nAdd a clinician-approved formulation.\n\n## Recommendations\n\nAdd practical, evidence-informed recommendations.');
  const [version, setVersion] = useState(1);
  const [preview, setPreview] = useState(false);
  const [compare, setCompare] = useState(false);
  const [approved, setApproved] = useState(false);
  const [copilotMessage, setCopilotMessage] = useState('Ready to help with this draft.');

  const runAction = (action: string) => {
    setVersion((value) => value + 1);
    setApproved(false);
    if (action === 'Shorten') setContent((value) => value.split('\n').filter(Boolean).slice(0, 5).join('\n'));
    if (action === 'Expand') setContent((value) => `${value}\n\n## Additional context\nAdd supporting evidence, functional impact and a clear plan for follow-up.`);
    if (action === 'GP format') setContent((value) => `Dear GP,\n\n${value}\n\nKind regards,\nNeuroAssess clinical team`);
    if (action === 'Patient friendly') setContent((value) => `## Your report\n\n${value}`);
    setCopilotMessage(`${action} applied. Version ${version + 1} is ready for review.`);
  };

  return <div className="animate-rise"><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="mono text-[10px] uppercase tracking-[.18em] text-[hsl(var(--primary))]">Clinical documentation</p><h2 className="mt-2 text-3xl tracking-[-.04em]">Report builder</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[hsl(var(--muted-foreground))]">Create a clinician-reviewed report from the consultation transcript, notes and assessment evidence.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => setPreview(!preview)} className={`inline-flex items-center gap-2 rounded-full px-4 py-2.5 text-xs font-semibold ${preview ? 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'border border-[hsl(var(--border))] bg-[hsl(var(--card))]'}`}><FileText size={14} /> Preview</button><button onClick={() => setCompare(!compare)} className={`inline-flex items-center gap-2 rounded-full px-4 py-2.5 text-xs font-semibold ${compare ? 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'border border-[hsl(var(--border))] bg-[hsl(var(--card))]'}`}><Columns2 size={14} /> Compare versions</button></div></div><div className="grid gap-6 xl:grid-cols-[260px_minmax(0,1fr)_280px]"><aside className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4"><div className="flex items-center justify-between"><h3 className="font-semibold">Template library</h3><span className="text-xs text-[hsl(var(--muted-foreground))]">{templates.length}</span></div><div className="mt-4 grid gap-1">{templates.map((template) => <button key={template.key} onClick={() => { setSelected(template); setApproved(false); }} className={`rounded-xl p-3 text-left ${selected.key === template.key ? 'bg-[hsl(var(--secondary))]' : 'hover:bg-[hsl(var(--secondary)/.5)]'}`}><div className="flex items-center justify-between gap-2"><span className="text-sm font-semibold">{template.name}</span>{selected.key === template.key && <ChevronRight size={14} className="text-[hsl(var(--primary))]" />}</div><span className="mt-1 block text-[10px] font-semibold uppercase tracking-[.12em] text-[hsl(var(--primary))]">{template.category}</span><span className="mt-1 block text-xs leading-5 text-[hsl(var(--muted-foreground))]">{template.detail}</span></button>)}</div></aside><main className="min-w-0"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><div className="flex flex-col justify-between gap-3 border-b border-[hsl(var(--border))] px-5 py-4 sm:flex-row sm:items-center"><div><p className="text-xs text-[hsl(var(--muted-foreground))]">{selected.category} · Version {version}</p><h3 className="mt-1 text-lg font-semibold">{selected.name}</h3></div><div className="flex items-center gap-2 text-xs"><span className={`rounded-full px-2.5 py-1 font-semibold ${approved ? 'bg-emerald-100 text-emerald-700' : 'bg-[hsl(var(--accent)/.3)] text-[hsl(var(--primary))]'}`}>{approved ? 'Approved' : 'Review required'}</span><span className="text-[hsl(var(--muted-foreground))]">Autosaved just now</span></div></div><div className="border-b border-[hsl(var(--border))] bg-[hsl(var(--secondary)/.35)] px-5 py-3"><label className="flex flex-wrap items-center gap-3 text-xs font-semibold">Source<select value={source} onChange={(event) => setSource(event.target.value)} className="rounded-lg border border-[hsl(var(--input))] bg-[hsl(var(--card))] px-2.5 py-2 font-normal"><option>Transcript and clinical notes</option><option>Clinical notes only</option><option>Assessment questionnaires</option><option>Manual input</option></select><button onClick={() => { setCopilotMessage('Draft generated from the selected source.'); setVersion((value) => value + 1); }} className="ml-auto inline-flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-2 text-xs font-semibold text-[hsl(var(--primary-foreground))]"><Sparkles size={14} /> Generate draft</button></label></div>{preview ? <article className="prose prose-sm max-w-none p-6"><MarkdownPreview content={content} /></article> : <textarea value={content} onChange={(event) => { setContent(event.target.value); setApproved(false); }} className="min-h-[560px] w-full resize-y border-0 bg-transparent p-6 font-mono text-sm leading-7 outline-none" aria-label="Report content" />}{compare && <div className="grid gap-4 border-t border-[hsl(var(--border))] bg-[hsl(var(--secondary)/.25)] p-5 md:grid-cols-2"><div><p className="text-xs font-semibold uppercase tracking-[.12em] text-[hsl(var(--muted-foreground))]">Version {Math.max(1, version - 1)}</p><p className="mt-3 whitespace-pre-wrap text-sm leading-6 text-[hsl(var(--muted-foreground))]">Previous draft snapshot is available for comparison.</p></div><div><p className="text-xs font-semibold uppercase tracking-[.12em] text-[hsl(var(--primary))]">Version {version}</p><p className="mt-3 whitespace-pre-wrap text-sm leading-6">{content.slice(0, 420)}...</p></div></div>}<div className="flex flex-wrap items-center justify-between gap-3 border-t border-[hsl(var(--border))] px-5 py-4"><span className="flex items-center gap-2 text-xs text-[hsl(var(--muted-foreground))]"><History size={14} /> {version} saved versions</span><div className="flex flex-wrap gap-2"><button onClick={() => { setContent(''); setApproved(false); }} className="inline-flex items-center gap-2 rounded-full border border-[hsl(var(--border))] px-4 py-2.5 text-xs font-semibold"><RotateCcw size={14} /> Clear draft</button><button onClick={() => setApproved(true)} className="inline-flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-2.5 text-xs font-semibold text-[hsl(var(--primary-foreground))]"><Check size={14} /> Approve report</button></div></div></div></main><aside className="grid content-start gap-6"><section className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--primary))] p-5 text-[hsl(var(--primary-foreground))]"><div className="flex items-center gap-2"><WandSparkles size={17} className="text-[hsl(var(--accent))]" /><h3 className="font-semibold">AI Copilot</h3></div><p className="mt-4 text-sm leading-6 text-[hsl(var(--primary-foreground)/.7)]">{copilotMessage}</p><div className="mt-5 grid gap-2">{['Rewrite', 'Expand', 'Shorten', 'Improve grammar', 'GP format', 'Patient friendly'].map((action) => <button key={action} onClick={() => runAction(action)} className="flex items-center justify-between rounded-xl bg-white/10 px-3 py-2.5 text-left text-xs font-semibold hover:bg-white/15">{action}<Sparkles size={13} className="text-[hsl(var(--accent))]" /></button>)}</div></section><section className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><h3 className="font-semibold">Clinical safeguards</h3><div className="mt-4 grid gap-3 text-xs leading-5 text-[hsl(var(--muted-foreground))]"><p className="flex gap-2"><Check size={14} className="mt-0.5 text-emerald-600" />Source material is retained with the draft.</p><p className="flex gap-2"><Check size={14} className="mt-0.5 text-emerald-600" />Every AI edit creates a new version.</p><p className="flex gap-2"><Check size={14} className="mt-0.5 text-emerald-600" />Approval is required before sharing.</p></div></section></aside></div></div>;
}

function MarkdownPreview({ content }: { content: string }) { return <>{content.split('\n').map((line, index) => line.startsWith('## ') ? <h2 key={index}>{line.slice(3)}</h2> : line.startsWith('# ') ? <h1 key={index}>{line.slice(2)}</h1> : <p key={index}>{line || '\u00a0'}</p>)}</>; }
