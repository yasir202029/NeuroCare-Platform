import { useRef, useState } from 'react';
import { Link } from 'wouter';
import { Activity, ArrowRight, CalendarDays, Check, ClipboardCheck, Download, FileText, FileUp, HeartPulse, MessageCircle, Pill, Send, Star, Upload, Video } from 'lucide-react';

type PatientPortalProps = { path: string };
type UploadCategory = 'ECG result' | 'Blood test' | 'School report' | 'GP letter' | 'Other document';

type Document = { id: number; name: string; category: UploadCategory; date: string; status: string };

const initialDocuments: Document[] = [
  { id: 1, name: 'Pre-assessment information.pdf', category: 'Other document', date: '04 Jun 2025', status: 'Available' },
  { id: 2, name: 'GP referral letter.pdf', category: 'GP letter', date: '04 Jun 2025', status: 'Available' },
];

const initialMessages = [
  { id: 1, sender: 'Dr Farah Malik', subject: 'Your assessment appointment', body: 'A quick note ahead of our appointment next Wednesday. Please bring any questions you would like us to discuss.', time: 'Today, 10:42', unread: true },
  { id: 2, sender: 'NeuroAssess team', subject: 'Your report is ready', body: 'Your assessment report has been reviewed and is ready to view in your reports area.', time: 'Yesterday', unread: true },
];

export function PatientPortalPage({ path }: PatientPortalProps) {
  if (path === '/patient/appointments') return <AppointmentsView />;
  if (path === '/patient/documents') return <DocumentsView />;
  if (path === '/patient/messages') return <MessagesView />;
  if (path === '/patient/reports') return <ReportsView />;
  if (path === '/patient/prescriptions') return <PrescriptionsView />;
  if (path === '/patient/invoices') return <InvoicesView />;
  if (path === '/patient/physical-health') return <PhysicalHealthView />;
  if (path === '/patient/feedback') return <FeedbackView />;
  return <DashboardView />;
}

function PortalHeading({ eyebrow, title, text, action }: { eyebrow: string; title: string; text: string; action?: React.ReactNode }) {
  return <div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="mono text-[10px] uppercase tracking-[.18em] text-[hsl(var(--primary))]">{eyebrow}</p><h2 className="mt-2 text-3xl tracking-[-.04em]">{title}</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[hsl(var(--muted-foreground))]">{text}</p></div>{action}</div>;
}

function Panel({ title, children, action }: { title: string; children: React.ReactNode; action?: React.ReactNode }) {
  return <section className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><div className="flex items-center justify-between border-b border-[hsl(var(--border))] px-5 py-4"><h3 className="font-semibold">{title}</h3>{action}</div><div className="p-5">{children}</div></section>;
}

function Stat({ label, value, detail, icon: Icon, accent = false }: { label: string; value: string; detail: string; icon: typeof Activity; accent?: boolean }) {
  return <div className={`rounded-2xl border p-5 ${accent ? 'border-[hsl(var(--primary))] bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'border-[hsl(var(--border))] bg-[hsl(var(--card))]'}`}><div className="flex items-start justify-between"><span className={`text-xs ${accent ? 'text-[hsl(var(--primary-foreground)/.7)]' : 'text-[hsl(var(--muted-foreground))]'}`}>{label}</span><Icon size={17} className={accent ? 'text-[hsl(var(--accent))]' : 'text-[hsl(var(--primary))]'} /></div><p className="mt-6 text-3xl font-semibold tracking-[-.04em]">{value}</p><p className={`mt-1 text-xs ${accent ? 'text-[hsl(var(--primary-foreground)/.6)]' : 'text-[hsl(var(--muted-foreground))]'}`}>{detail}</p></div>;
}

function DashboardView() {
   return <div className="animate-rise"><PortalHeading eyebrow="Wednesday, 18 June 2025" title="Good morning, Maya." text="Here is the latest from your assessment journey." action={<Link href="/patient/appointments" className="inline-flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-semibold text-[hsl(var(--primary-foreground))]">Book an appointment <ArrowRight size={15} /></Link>} /><div className="grid gap-4 md:grid-cols-3"><Stat label="Assessment progress" value="68%" detail="ADHD assessment" icon={ClipboardCheck} accent /><Stat label="Next appointment" value="18 Jun" detail="Today at 09:30" icon={CalendarDays} /><Stat label="Unread messages" value="2" detail="From your care team" icon={MessageCircle} /></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><Panel title="Your assessment journey" action={<Link href="/patient/assessment" className="text-xs font-semibold text-[hsl(var(--primary))]">View assessment <ArrowRight className="ml-1 inline" size={13} /></Link>}><div className="flex items-center justify-between text-sm"><span className="font-semibold">ADHD assessment</span><span className="font-semibold text-[hsl(var(--primary))]">68%</span></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-[hsl(var(--secondary))]"><div className="h-full w-[68%] rounded-full bg-[hsl(var(--primary))]" /></div><div className="mt-5 grid gap-4 sm:grid-cols-3">{['History received', 'Questionnaires', 'Clinical interview'].map((step, index) => <div key={step} className="flex items-center gap-2 text-sm"><span className={`grid h-7 w-7 place-items-center rounded-full ${index < 2 ? 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'border border-[hsl(var(--border))] text-[hsl(var(--muted-foreground))]'}`}>{index < 2 ? <Check size={14} /> : '3'}</span><span className={index < 2 ? '' : 'text-[hsl(var(--muted-foreground))]'}>{step}</span></div>)}</div></Panel><Panel title="Next appointment"><div className="rounded-xl bg-[hsl(var(--secondary)/.55)] p-4"><div className="flex items-start justify-between"><div><p className="font-semibold">ADHD assessment</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">with Dr Farah Malik</p></div><span className="rounded-full bg-[hsl(var(--card))] px-2.5 py-1 text-[11px] font-semibold text-[hsl(var(--primary))]">Confirmed</span></div><div className="mt-7 grid grid-cols-2 gap-4 text-sm"><div><p className="mono text-[10px] uppercase text-[hsl(var(--muted-foreground))]">Date</p><p className="mt-1 font-semibold">Today, 18 Jun</p></div><div><p className="mono text-[10px] uppercase text-[hsl(var(--muted-foreground))]">Time</p><p className="mt-1 font-semibold">09:30</p></div></div><Link href="/patient/session/demo" className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))]"><Video size={15} /> Join when ready</Link></div></Panel></div><div className="mt-6 grid gap-6 md:grid-cols-3"><QuickLink href="/patient/documents" icon={Upload} title="Upload documents" text="ECGs, blood results and reports" /><QuickLink href="/patient/physical-health" icon={HeartPulse} title="Physical health" text="Keep your readings together" /><QuickLink href="/patient/feedback" icon={Star} title="Share feedback" text="Tell us how your care feels" /></div></div>;
}

function QuickLink({ href, icon: Icon, title, text }: { href: string; icon: typeof Upload; title: string; text: string }) { return <Link href={href} className="group rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 hover:-translate-y-0.5 hover:shadow-[var(--shadow-sm)]"><Icon size={19} className="text-[hsl(var(--primary))]" /><h3 className="mt-8 font-semibold">{title} <ArrowRight className="ml-1 inline transition-transform group-hover:translate-x-1" size={14} /></h3><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">{text}</p></Link>; }

function AppointmentsView() {
  const [booked, setBooked] = useState(false);
  return <div className="animate-rise"><PortalHeading eyebrow="Your care" title="Appointments" text="Book and manage your conversations with the NeuroAssess team." action={<button onClick={() => setBooked(true)} className="inline-flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-semibold text-[hsl(var(--primary-foreground))]"><CalendarDays size={15} /> New appointment</button>} />{booked && <div className="mb-6 rounded-2xl border border-[hsl(var(--primary)/.25)] bg-[hsl(var(--secondary)/.45)] p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="font-semibold">Choose your appointment</p><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">We will confirm the clinician and time by email.</p></div><div className="flex flex-wrap gap-3"><select className="rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--card))] px-3 py-2.5 text-sm"><option>ADHD assessment</option><option>Autism assessment</option><option>Follow-up appointment</option></select><input type="date" className="rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--card))] px-3 py-2.5 text-sm" /><button onClick={() => setBooked(false)} className="rounded-full bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-semibold text-[hsl(var(--primary-foreground))]">Request slot</button></div></div></div>}<Panel title="Upcoming"><div className="grid gap-3"><AppointmentRow title="ADHD assessment" detail="with Dr Farah Malik · Video consultation" date="Today, 18 Jun" time="09:30" status="Confirmed" /><AppointmentRow title="Feedback appointment" detail="with Dr Farah Malik · Video consultation" date="26 Jun 2025" time="14:00" status="Requested" /></div></Panel><div className="mt-6"><Panel title="Appointment history"><div className="grid gap-3"><AppointmentRow title="Initial consultation" detail="Dr Farah Malik · Video consultation" date="04 Jun 2025" time="11:00" status="Completed" /><AppointmentRow title="Pre-assessment call" detail="NeuroAssess team · Phone" date="30 May 2025" time="16:30" status="Completed" /></div></Panel></div></div>;
}

function AppointmentRow({ title, detail, date, time, status }: { title: string; detail: string; date: string; time: string; status: string }) { return <div className="grid gap-4 rounded-xl border border-[hsl(var(--border))] p-4 md:grid-cols-[1fr_auto_auto] md:items-center"><div className="flex items-center gap-4"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]"><CalendarDays size={18} /></span><div><p className="font-semibold">{title}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{detail}</p></div></div><div className="text-sm md:text-right"><p className="font-semibold">{date}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{time}</p></div><span className="rounded-full bg-[hsl(var(--secondary))] px-2.5 py-1 text-center text-[11px] font-semibold text-[hsl(var(--primary))]">{status}</span></div>; }

function DocumentsView() {
  const [documents, setDocuments] = useState(initialDocuments);
  const [category, setCategory] = useState<UploadCategory>('Other document');
  const inputRef = useRef<HTMLInputElement>(null);
  const addFiles = (files: FileList | null) => { if (!files) return; setDocuments((current) => [...Array.from(files).map((file, index) => ({ id: Date.now() + index, name: file.name, category, date: 'Today', status: 'Uploaded' })), ...current]); };
  return <div className="animate-rise"><PortalHeading eyebrow="Your documents" title="Documents" text="Upload the information your clinical team needs. Files are stored securely." action={<button onClick={() => inputRef.current?.click()} className="inline-flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-semibold text-[hsl(var(--primary-foreground))]"><Upload size={15} /> Upload document</button>} /><input ref={inputRef} type="file" multiple className="hidden" onChange={(event) => addFiles(event.target.files)} /><Panel title="Upload a document"><div className="grid gap-4 md:grid-cols-[1fr_auto] md:items-end"><label className="grid gap-2 text-sm font-semibold">Document type<select value={category} onChange={(event) => setCategory(event.target.value as UploadCategory)} className="rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] px-3 py-3 font-normal"><option>ECG result</option><option>Blood test</option><option>School report</option><option>GP letter</option><option>Other document</option></select></label><button onClick={() => inputRef.current?.click()} className="inline-flex items-center justify-center gap-2 rounded-full border border-[hsl(var(--primary))] px-4 py-3 text-sm font-semibold text-[hsl(var(--primary))]"><FileUp size={15} /> Choose files</button></div><p className="mt-4 text-xs text-[hsl(var(--muted-foreground))]">PDF, JPG or PNG · Maximum 10 MB per file</p></Panel><div className="mt-6"><Panel title={`${documents.length} documents`}><div className="grid gap-2">{documents.map((document) => <div key={document.id} className="flex items-center gap-4 rounded-xl p-3 hover:bg-[hsl(var(--secondary)/.35)]"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]"><FileText size={18} /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{document.name}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{document.category} · {document.date}</p></div><span className="rounded-full bg-[hsl(var(--secondary))] px-2.5 py-1 text-[11px] font-semibold text-[hsl(var(--primary))]">{document.status}</span><button aria-label={`Download ${document.name}`} className="rounded-full p-2 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><Download size={16} /></button></div>)}</div></Panel></div></div>;
}

function MessagesView() {
  const [messages, setMessages] = useState(initialMessages);
  const [body, setBody] = useState('');
  const [sent, setSent] = useState(false);
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!body.trim()) return; setMessages((current) => [{ id: Date.now(), sender: 'You', subject: 'Message to care team', body, time: 'Just now', unread: false }, ...current]); setBody(''); setSent(true); };
  return <div className="animate-rise"><PortalHeading eyebrow="Secure inbox" title="Messages" text="A private line to your NeuroAssess care team." /><div className="grid gap-6 lg:grid-cols-[1fr_.8fr]"><Panel title="Your inbox"><div className="grid gap-2">{messages.map((message) => <div key={message.id} className={`rounded-xl p-4 ${message.unread ? 'bg-[hsl(var(--secondary)/.5)]' : 'border border-[hsl(var(--border))]'}`}><div className="flex items-start justify-between gap-4"><div><p className="font-semibold">{message.subject}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{message.sender} · {message.time}</p></div>{message.unread && <span className="h-2 w-2 rounded-full bg-[hsl(var(--accent))]" />}</div><p className="mt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">{message.body}</p></div>)}</div></Panel><Panel title="Send a secure message"><form onSubmit={submit}><label className="grid gap-2 text-sm font-semibold">Message<textarea required value={body} onChange={(event) => setBody(event.target.value)} className="min-h-40 rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] p-3 font-normal outline-none focus:border-[hsl(var(--primary))]" placeholder="Write to your care team..." /></label><button className="mt-4 inline-flex w-full items-center justify-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))]"><Send size={15} /> Send securely</button>{sent && <p className="mt-3 text-xs font-semibold text-[hsl(var(--primary))]">Your message has been sent securely.</p>}</form></Panel></div></div>;
}

function ReportsView() { return <div className="animate-rise"><PortalHeading eyebrow="Your care" title="Reports" text="Reports and letters shared with you by your clinical team." /><Panel title="Available reports"><div className="grid gap-3"><DownloadRow icon={FileText} title="ADHD assessment report" detail="14 Jun 2025 · 2.4 MB · Reviewed by Dr Farah Malik" /><DownloadRow icon={FileText} title="Care recommendations" detail="14 Jun 2025 · 840 KB · Shared with you" /><DownloadRow icon={FileText} title="GP letter" detail="14 Jun 2025 · 320 KB · Ready to download" /></div></Panel></div>; }
function DownloadRow({ icon: Icon, title, detail }: { icon: typeof FileText; title: string; detail: string }) { return <div className="flex items-center gap-4 rounded-xl border border-[hsl(var(--border))] p-4"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]"><Icon size={18} /></span><div className="min-w-0 flex-1"><p className="font-semibold">{title}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{detail}</p></div><button aria-label={`Download ${title}`} className="rounded-full p-2 text-[hsl(var(--primary))] hover:bg-[hsl(var(--secondary))]"><Download size={17} /></button></div>; }

function PrescriptionsView() { return <div className="animate-rise"><PortalHeading eyebrow="Medication" title="Prescriptions" text="View your current prescriptions and delivery status." /><Panel title="Current prescriptions"><div className="grid gap-3"><PrescriptionRow name="Lisdexamfetamine" detail="30 mg · Once each morning" status="Delivered" /><PrescriptionRow name="Medication review" detail="Next review: 02 Jul 2025" status="Booked" /></div></Panel></div>; }
function PrescriptionRow({ name, detail, status }: { name: string; detail: string; status: string }) { return <div className="flex items-center gap-4 rounded-xl border border-[hsl(var(--border))] p-4"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]"><Pill size={18} /></span><div className="flex-1"><p className="font-semibold">{name}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{detail}</p></div><span className="rounded-full bg-[hsl(var(--secondary))] px-2.5 py-1 text-[11px] font-semibold text-[hsl(var(--primary))]">{status}</span></div>; }

function InvoicesView() { return <div className="animate-rise"><PortalHeading eyebrow="Your account" title="Invoice history" text="A clear record of your assessment and appointment payments." /><Panel title="Invoices"><div className="grid gap-2"><InvoiceRow description="ADHD assessment — balance" date="18 Jun 2025" amount="£675.00" status="Due" /><InvoiceRow description="ADHD assessment — deposit" date="04 Jun 2025" amount="£225.00" status="Paid" /><InvoiceRow description="Initial consultation" date="30 May 2025" amount="£145.00" status="Paid" /></div></Panel></div>; }
function InvoiceRow({ description, date, amount, status }: { description: string; date: string; amount: string; status: string }) { return <div className="grid gap-3 rounded-xl p-3 hover:bg-[hsl(var(--secondary)/.35)] sm:grid-cols-[1fr_auto_auto_auto] sm:items-center"><div><p className="text-sm font-semibold">{description}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{date} · INV-1048</p></div><span className="text-sm font-semibold">{amount}</span><span className="rounded-full bg-[hsl(var(--secondary))] px-2.5 py-1 text-center text-[11px] font-semibold text-[hsl(var(--primary))]">{status}</span><button aria-label={`Download invoice for ${description}`} className="inline-flex items-center gap-1 text-xs font-semibold text-[hsl(var(--primary))]"><Download size={14} /> Invoice</button></div>; }

function PhysicalHealthView() {
  const [saved, setSaved] = useState(false);
  return <div className="animate-rise"><PortalHeading eyebrow="Your wellbeing" title="Physical health" text="Keep your health readings together for medication reviews and clinical appointments." action={<span className="inline-flex items-center gap-2 rounded-full bg-[hsl(var(--secondary))] px-3 py-2 text-xs font-semibold text-[hsl(var(--primary))]"><Activity size={14} /> Latest review: 04 Jun</span>} /><div className="grid gap-6 lg:grid-cols-[1fr_.85fr]"><Panel title="Add a reading"><form onSubmit={(event) => { event.preventDefault(); setSaved(true); }} className="grid gap-4 sm:grid-cols-2"><HealthField label="Blood pressure" placeholder="e.g. 118/76" /><HealthField label="Pulse" placeholder="e.g. 72 bpm" /><HealthField label="Weight" placeholder="e.g. 68 kg" /><HealthField label="Height" placeholder="e.g. 170 cm" /><HealthField label="Sleep" placeholder="e.g. 7 hours" /><HealthField label="Appetite" placeholder="Good, reduced or increased" /><label className="grid gap-2 text-sm font-semibold sm:col-span-2">Side effects<textarea className="min-h-24 rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] p-3 font-normal" placeholder="Anything you would like your clinician to know?" /></label><button className="inline-flex items-center justify-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))] sm:col-span-2"><HeartPulse size={15} /> Save health reading</button>{saved && <p className="text-xs font-semibold text-[hsl(var(--primary))] sm:col-span-2">Reading saved for your care team.</p>}</form></Panel><Panel title="Recent readings"><div className="grid gap-4 text-sm"><HealthRow label="Blood pressure" value="118/76 mmHg" date="04 Jun 2025" /><HealthRow label="Pulse" value="72 bpm" date="04 Jun 2025" /><HealthRow label="Weight" value="68 kg" date="04 Jun 2025" /><HealthRow label="BMI" value="23.5" date="04 Jun 2025" /></div></Panel></div></div>;
}
function HealthField({ label, placeholder }: { label: string; placeholder: string }) { return <label className="grid gap-2 text-sm font-semibold">{label}<input className="rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] px-3 py-3 font-normal" placeholder={placeholder} /></label>; }
function HealthRow({ label, value, date }: { label: string; value: string; date: string }) { return <div className="flex items-center justify-between border-b border-[hsl(var(--border))] pb-3 last:border-0"><div><p className="font-semibold">{label}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{date}</p></div><span className="font-semibold text-[hsl(var(--primary))]">{value}</span></div>; }

function FeedbackView() {
  const [rating, setRating] = useState(0);
  const [submitted, setSubmitted] = useState(false);
  return <div className="animate-rise"><PortalHeading eyebrow="Your voice matters" title="Feedback" text="Help us understand what is working and where we can improve." /><div className="max-w-2xl"><Panel title="How was your experience?"><form onSubmit={(event) => { event.preventDefault(); setSubmitted(true); }}><p className="text-sm font-semibold">Rate your care</p><div className="mt-3 flex gap-2">{[1, 2, 3, 4, 5].map((value) => <button key={value} type="button" onClick={() => setRating(value)} aria-label={`${value} stars`} className={`rounded-full p-2 ${value <= rating ? 'text-[hsl(var(--accent))]' : 'text-[hsl(var(--muted-foreground))]'}`}><Star size={24} fill="currentColor" /></button>)}</div><label className="mt-5 grid gap-2 text-sm font-semibold">Tell us more<textarea className="min-h-32 rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] p-3 font-normal" placeholder="What would you like us to know?" /></label><button disabled={!rating} className="mt-5 inline-flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-5 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))] disabled:opacity-50"><Send size={15} /> Submit feedback</button>{submitted && <p className="mt-4 text-sm font-semibold text-[hsl(var(--primary))]">Thank you. Your feedback has been received.</p>}</form></Panel></div></div>;
}
